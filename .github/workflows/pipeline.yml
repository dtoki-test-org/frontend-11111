name: Frontend Pipeline

on:
  push:
    branches: [ master ]

jobs:
# 
  build:
    runs-on: ubuntu-latest
    steps:
    # Checkout the code
      - uses: actions/checkout@v2
    # Run Tests
      - name: Run Unit Tests
        uses: actions/setup-node@v1
        with: 
          node-version: 12.x
      - run: |
          yarn
          yarn test --watchAll=false
    # Build Code
      - name: Build Static Site
        uses: actions/setup-node@v1
        with: 
          node-version: 12.x  
      - run: |
          yarn
          yarn build
          ls -al
        env:
          # TODO: how do you have the right config when templated?
          REACT_APP_CONFIG: prod
    # Upload build to github artifact
      - name: Upload build artifact to Github
        uses: actions/upload-artifact@v1
        with:
          name: build-artifact
          path: build/
    env:
      CI: true
# TODO: Deploy for stage
  deploy:
    name: Upload to Amazon S3
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifact from build step
        uses: actions/download-artifact@v1
        env:
          region: us-west-2
          bucket: dtoki-0914-app.cmt.dev
        with:
          name: build-artifact
      # Old sync settings
      # - uses: jakejarvis/s3-sync-action@master
      #   with:
      #     args: --delete
      #   env:
      #     AWS_S3_BUCKET: guide.commit.dev
      #     AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     AWS_REGION: 'us-west-2'
      #     SOURCE_DIR: 'build-artifact'

      # Invalidate Cache
      # Old cloudfront invalidate settings
      # - name: Invalidate Cloudfront Cache
      #   uses: chetan/invalidate-cloudfront-action@master
      #   env:
      #     DISTRIBUTION: ${{ secrets.AWS_CLOUDFRONT_DISTRIBUTION_ID }}
      #     PATHS: '/*'
      #     AWS_REGION: 'us-east-2'
      #     AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      
      # Configure Aws account for S3 Sync
      - name: Configure AWS credentials for S3 sync
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.region }}
      
      # Deploy to s3
      - name: Copy files to the test website with the AWS CLI
        run: |
          cd build-artifact
          aws s3 sync . s3://${{ env.bucket }}

      # Invalidate Cloudfront
      # - name: Invalidate Cloudfront
      #   run: |
      #     export DIST_ID=$(aws cloudfront list-distributions --query "DistributionList.Items[?Aliases.Items[?@=='<< parameters.bucket >>']].Id | [0]" | tr -d '"')
      #     aws cloudfront create-invalidation --distribution-id ${DIST_ID} --paths "/*"
# TODO: Deploy for productions